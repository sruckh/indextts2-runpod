# Core dependencies for IndexTTS RunPod Serverless
# Note: The main IndexTTS dependencies are installed from the GitHub repo

# PyTorch with CUDA support
torch>=2.5.0
torchaudio>=2.5.0

# RunPod serverless
runpod>=1.6.1

# AWS S3 compatible storage
boto3>=1.34.0

# HTTP server
uvicorn[standard]
pydantic>=2.0.0
python-multipart

# Utilities
tqdm
numpy
einops
safetensors
huggingface-hub
modelscope
pyyaml
transformers>=4.30.0
accelerate

# Audio processing
# (ffmpeg is installed via apt in Dockerfile)
